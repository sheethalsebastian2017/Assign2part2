extends ../layout

block content
  .container
    h1 All Surveys

    if isAuthenticated
      a(href="/surveys/new") Create Survey

    ul
      each survey in surveys
        li
          a(href=`/surveys/${survey._id}`)= survey.title